     pipeline {
        agent none
        stages {
         
          stage("build & sonarqube") {
            agent any
            steps {
              withSonarQubeEnv('sonarqube-project') {
                sh 'mvn clean package sonar:sonar'
              }
            }
          }
        }
      }
